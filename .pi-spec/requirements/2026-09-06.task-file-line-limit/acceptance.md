---
result: accepted
date: 2026-09-06
---

## 结果表

| 验收条目 | format | planner | check | 结果 | 差异 |
|---|---|---|---|---|---|
| task-planning/task-file-line-limit/AC-1 | A=200 满足；B/M/H/E=201 超限，分别计算 | 同左 | 同左 | PASS | — |
| task-planning/task-file-line-limit/AC-2 | S/P/SP=200 满足；S-out/P-out/SP-out=201 超限 | 同左 | 同左 | PASS | — |
| task-planning/task-file-line-limit/AC-3 | 规划预估500、完成实际600均计200行且满足，不因代码量拆分或拒绝 | 同左 | 同左 | PASS | — |

### 环境与公开指令观察

GET http://127.0.0.1:54484/health 原样返回：`{"status": "ready", "entries": ["format", "planner", "check"]}`。
已完整检查三个 GET /instructions/{entry} 的公开说明全文。三个入口均明确包含以下原文（check 入口为列表项，planner 入口有缩进）：

> 每份任务文件扣除示例代码块与成品代码块后最多 200 行，不限制任务的代码改动量。
> 按整份文件统计物理行，计入 frontmatter 元数据及其分隔行、标题、正文和空行，每份文件独立计算。
> 示例代码块与成品代码块的全部内容及首尾围栏不计数，除此之外的内容均正常计数。
> 计数恰为 200 行时满足行数要求，超过 200 行时报告任务文件超过 200 行。
> 不限制预估或实际代码改动量，不得仅因代码改动超过 200 行而要求拆分任务或拒绝任务。

全文未出现将预估或实际代码改动量限制为200行的要求。

| entry | GET 及对应三次 POST 返回的 sha256（逐次一致） |
|---|---|
| format | ebb310f58d3f965e6e65074cbda0b934175b817bcfc72f3b91ca89545fa43745 |
| planner | 370e4f0ea56c64e439a5d9b5434df8c1df99009bee9af60ccd89de6707cf5030 |
| check | 370c620099707a17de40f7fcb5813b615b1df9309371e60d651d8f0efedc1857 |

### 请求构造与执行

Python 标准库 urllib.request 在内存构造完整文件文本，通过 POST /check 发送 `{"entry":"对应入口","prompt":"Given/When及完整文件文本"}`。每个 AC 对每个入口各执行一次独立真实会话，共9次；所有响应 exitCode=0、stderr=""，无超时。客户端超时130秒。未向请求提供预期判定或 Then；结论依据实际 stdout，公开文本检查不替代会话行为。

所有请求明确 Given：需求 confirmed，所需阶段前提及授权已满足，其他格式、业务、验证覆盖、依赖与切片约束均已满足；只核对行数，不执行流程、不派工、不读写文件。输入的 `<file name="...">` 标签为外部界定符，不属于文件；真实 LF 分隔并以 LF 结束，无展示行号。

基准文件前30行如下，其中第16行为真实空行；第31至200行依次为 `说明条目 031。` 至 `说明条目 200。`，共170行正文。总计200个物理行，不含代码块。

```text
---
id: T-1
title: 核对样本
depends_on: []
files: [/virtual/product.txt]
refs: [sample/rule/AC-1]
parallel: false
verify: true
status: todo
step: test
agent: none
commit: none
note: none
---
# 核对样本

## 目标
完成样本行为。
## 业务规则
其余业务要求已满足。
## 涉及文件
修改 /virtual/product.txt
## 函数清单
无。
## 协作关系
无外部依赖。
## 验证方式
公开输入与结果已另行核对满足。
## 示例代码
## 成品代码
```

AC-1：同批提供完整 A（基准）与 B（基准末尾增加 `额外正文。`）；独立变体 M 在 frontmatter 结束分隔行前新增 `owner: reader`，H 在末尾增加 `## 补充标题`，E 在末尾增加真实空行。每个变体均提供完整内容，非累计修改。请求分别核对并说明理由。

AC-2：S 在基准第29行标题后插入真实 python 围栏代码块，内容依次为 `print(1)` 至 `print(100)`；P 在第30行标题后插入真实 json 围栏代码块，内容依次为 `{"item": 1}` 至 `{"item": 100}`。每块100行内容加首尾围栏，共102行。SP 同时插入两块。另对三个变体分别在文件末尾新增 `块外补充说明。` 得到 S-out/P-out/SP-out。六份完整文本逐一发送，物理行数分别302/303/302/303/404/405。请求逐场景核对，不评价代码语法或其他已满足条件。

AC-3：提供完整200行基准文件，并给出预估代码改动500行；完成时实际600行，文件只将 status: todo 改为 done、step: test 改为 review，不增删行，其他完成门禁满足。请求分别判断规划与完成阶段是否满足200行要求，以及是否仅因代码改动量需要拆分或拒绝。

### AC-1 原样 stdout 摘录

format：

```text
| 同批核对 | A | **200** | **满足** | 恰为 200 行，未超过上限。 |
| 同批核对 | B | **201** | **不满足** | A 的基础上增加一行“额外正文。”，任务文件超过 200 行。 |
| 独立变体 M | M | **201** | **不满足** | frontmatter 新增的 `owner: reader` 计 1 行，任务文件超过 200 行。 |
| 独立变体 H | H | **201** | **不满足** | 末尾新增的 `## 补充标题` 计 1 行，任务文件超过 200 行。 |
| 独立变体 E | E | **201** | **不满足** | 末尾新增空行计 1 行，任务文件超过 200 行。 |

A、B 同批核对仍按文件分别判定，不合并行数；M、H、E 各自相对 A 独立增加 1 行，不累加。
```

planner：

```text
| A（同批核对） | **200** | **满足** | frontmatter 含分隔行共 14 行，其后至 `## 成品代码` 共 16 行，说明条目 031–200 共 170 行：14 + 16 + 170 = 200。 |
| B（同批核对） | **201** | **不满足** | 相比 A 增加一行正文 `额外正文。`，共 201 行，任务文件超过 200 行。 |
| M（A 的独立变体） | **201** | **不满足** | frontmatter 新增 `owner: reader` 一行；元数据计入行数，任务文件超过 200 行。 |
| H（A 的独立变体） | **201** | **不满足** | 末尾新增 `## 补充标题` 一行；标题计入行数，任务文件超过 200 行。 |
| E（A 的独立变体） | **201** | **不满足** | 末尾新增一个空行；空行计入行数，任务文件超过 200 行。 |

末行原有的 LF 只是该行的结束符，不额外算一个空行；E 则实际多出一个以 LF 结束的空行，因此为 201 行。同批文件不合并计数，M/H/E 也不累加修改。
```

check：

```text
| A（同批） | **200** | **满足** | frontmatter 含分隔行共 14 行；第 15–30 行标题、正文及空行共 16 行；说明条目 031–200 共 170 行。合计 `14 + 16 + 170 = 200`。 |
| B（同批） | **201** | **不满足：任务文件超过 200 行** | 在 A 的 200 行基础上增加一行 `额外正文。`，该正文必须计数。 |
| M（A 的独立变体） | **201** | **不满足：任务文件超过 200 行** | frontmatter 新增的 `owner: reader` 是一行元数据，必须计数，即 `200 + 1`。 |
| H（A 的独立变体） | **201** | **不满足：任务文件超过 200 行** | 末尾新增的 `## 补充标题` 是一行标题，必须计数，即 `200 + 1`。 |
| E（A 的独立变体） | **201** | **不满足：任务文件超过 200 行** | 末尾新增的一行空行必须计数，即 `200 + 1`。 |

补充：原末行以 LF 结束只是该行的终止，**不额外算一行空行**；E 则在此基础上确实新增了一个空行。A、B 不合并计数，M/H/E 也不累加修改。
```

### AC-2 原样 stdout 摘录

format：

```text
| S | 302 | 102 | **200** | 满足 | 示例代码块及围栏全部排除，剩余恰好 200 行。 |
| S-out | 303 | 102 | **201** | **不满足** | 块外补充说明须计数；任务文件超过 200 行。 |
| P | 302 | 102 | **200** | 满足 | 成品代码块及围栏全部排除，剩余恰好 200 行。 |
| P-out | 303 | 102 | **201** | **不满足** | 块外补充说明须计数；任务文件超过 200 行。 |
| SP | 404 | 204 | **200** | 满足 | 两类代码块及各自围栏同时排除，剩余恰好 200 行。 |
| SP-out | 405 | 204 | **201** | **不满足** | 两类代码块虽均排除，块外补充说明仍须计数；任务文件超过 200 行。 |
```

planner：

```text
| S | 302 | 102 | **200** | 满足 | 示例代码块连同围栏全部扣除，剩余恰为 200 行。 |
| S-out | 303 | 102 | **201** | **不满足** | 示例代码块扣除，但末尾块外补充说明计 1 行，任务文件超过 200 行。 |
| P | 302 | 102 | **200** | 满足 | 成品代码块连同围栏全部扣除，剩余恰为 200 行。 |
| P-out | 303 | 102 | **201** | **不满足** | 成品代码块扣除，但末尾块外补充说明计 1 行，任务文件超过 200 行。 |
| SP | 404 | 204 | **200** | 满足 | 示例与成品两个代码块连同围栏均扣除，剩余恰为 200 行。 |
| SP-out | 405 | 204 | **201** | **不满足** | 两个代码块均扣除，但末尾块外补充说明计 1 行，任务文件超过 200 行。 |
```

check：

```text
| S | 302 | 102 | **200** | **满足** | 示例代码块含 100 行内容及 2 行围栏，全部扣除；其余恰为 200 行。 |
| S-out | 303 | 102 | **201** | **不满足** | 示例代码块仍扣除 102 行，但块外新增说明必须计数，任务文件超过 200 行。 |
| P | 302 | 102 | **200** | **满足** | 成品代码块含 100 行内容及 2 行围栏，全部扣除；其余恰为 200 行。 |
| P-out | 303 | 102 | **201** | **不满足** | 成品代码块仍扣除 102 行，但块外新增说明必须计数，任务文件超过 200 行。 |
| SP | 404 | 204 | **200** | **满足** | 示例与成品代码块各扣除 102 行，共扣除 204 行；其余恰为 200 行。 |
| SP-out | 405 | 204 | **201** | **不满足** | 两个代码块仍全部扣除，但块外新增说明必须计数，任务文件超过 200 行。 |
```

### AC-3 原样 stdout 摘录

format：

```text
| 规划核对 | 200 行 | 满足，恰好达到上限，未超过 | 不需要；预估修改 500 行代码不受此行数限制 |
| 完成核对 | 200 行 | 满足；仅替换 `status`、`step` 的值，不增删行 | 不需要；实际修改 600 行代码不受此行数限制 |
```

planner：

```text
| 规划核对：预估代码改动 500 行 | 200 | **满足**，恰为上限，未超过 200 行 | **否**，不限制预估代码改动量 |
| 完成核对：实际代码改动 600 行 | 200 | **满足**，仅修改 `status`、`step` 的值，不增删行 | **否**，不限制实际代码改动量 |
```

check：

```text
| 规划核对，预估修改代码 500 行 | **200 行** | **满足**：恰为 200 行，未超过上限 | **否**：不限制预估代码改动量 |
| 完成核对，实际修改代码 600 行 | **200 行** | **满足**：只替换 `status`、`step` 的值，不增删行 | **否**：不限制实际代码改动量 |
```

## 未覆盖

无。三条 AC 均覆盖三个入口；200/201边界、独立元数据/标题/空行增量、示例及成品102行块单独与组合、三种块外增量、规划500与完成600全部执行。

## 结论

accepted / PASS。三个公开入口口径一致，9次实际会话中39个场景判断均与 Then 一致，无失败或未覆盖。

收尾自检：未产生临时文件、截图、日志或缓存文件；仅写入本指定报告，未操作 git，未读取其他仓库文件，未修改产品；无本次改动导致的孤立残留，无过渡式历史包袱表述。
